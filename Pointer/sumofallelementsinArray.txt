#include<bits/stdc++.h>
using namespace std;

void sumofall(int *arr,int n)
{
    int sum=*arr;
    for(int i=1;i<n;i++)
    {
        sum+=*(arr+i);
    }
    cout<<sum;
}



int main()
{
    int arr[]={1,3,3,4,5};
    int n=sizeof(arr)/sizeof(arr[0]);
    sumofall(arr,n);
}